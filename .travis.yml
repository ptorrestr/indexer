language: python
cache:
  directories:
    - "$HOME/.cache/pip"
    - "$HOME/.pyenv"
matrix:
  include:
    - os: linux
      dist: trusty
      python: 3.5
    - os: osx
      language: objective-c
      env: PYENV_VERSION=3.5.2
install:
  - source .travis/install.sh
script:
  - .travis/run.sh
