language: python
cache:
  directories:
    - "$HOME/.cache/pip"
    - "$HOME/.pyenv"
matrix:
  include:
    - os: linux
      dist: trusty
      python: 3.5
    - os: osx
      python: 3.5
install:
  - source .travis/install.sh
script:
  - .travis/run.sh
